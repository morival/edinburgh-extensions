import React from 'react';
import { MainContainer } from '../elements';


export const Container = ({ children }) => {
    return (
        <MainContainer>
            {children}
        </MainContainer>
    )
}